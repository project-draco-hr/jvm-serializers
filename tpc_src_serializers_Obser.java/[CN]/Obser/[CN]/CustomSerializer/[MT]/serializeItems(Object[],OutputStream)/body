{
  MediaContentCustom[] data=new MediaContentCustom[items.length];
  for (int i=0; i < items.length; i++)   data[i]=new MediaContentCustom((MediaContent)items[i]);
  super.serializeItems((MediaContent[])(Object)data,outStream);
}
